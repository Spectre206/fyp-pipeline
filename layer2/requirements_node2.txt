# Layer 2 — Node 2 (ai-brain-node) Python dependencies
# Install inside a virtual environment: pip install -r requirements_node2.txt

# Core
pika>=1.3                        # RabbitMQ AMQP client
requests>=2.31                   # Ollama REST API calls

# ChromaDB + embeddings
chromadb>=0.4
sentence-transformers>=2.3       # all-MiniLM-L6-v2 embedding model

# Validation
pydantic>=2.0

# Prometheus metrics
prometheus-client>=0.17

# Utilities
python-dotenv>=1.0
numpy>=1.24
