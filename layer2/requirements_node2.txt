# Layer 2 — Node 2 (ai-brain-node) Python dependencies
# Install inside a virtual environment: pip install -r requirements_node2.txt

# Message broker (connects to stream-node: 5672)
pika==1.3.2

# LLM inference client (calls localhost:11434)
requests==2.31.0

# Vector database client
chromadb==0.4.24

# Embeddings for ChromaDB RAG
sentence-transformers==2.7.0

# Schema validation (shared with Node 1)
pydantic==2.7.0

# Monitoring
prometheus-client==0.20.0

# Utilities
python-dotenv==1.0.1
structlog==24.2.0
numpy==1.26.4