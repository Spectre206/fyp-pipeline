# Layer 1 — Node 1 (stream-node) Python dependencies
# Install inside a virtual environment: pip install -r requirements_node1.txt

# Core
pydantic>=2.0
pika>=1.3           # RabbitMQ AMQP client
numpy>=1.24
pandas>=2.0

# Anomaly Detection
scikit-learn>=1.3   # IsolationForest, RandomForestClassifier
river>=0.21         # PSI streaming computation

# Prometheus metrics
prometheus-client>=0.17

# Utilities
python-dotenv>=1.0
